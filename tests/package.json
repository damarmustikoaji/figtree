{
  "name": "hellokognisia-web-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "localhost-serve": "npx serve .. -l 3000",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@axe-core/playwright": "^4.9.0",
    "serve": "^14.2.0",
    "wait-on": "^7.2.0"
  }
}
